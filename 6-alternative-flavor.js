// Alternative syntax for modules.
// At the end of the day, it is the same syntax. You can choose wich one to us depending on the situation.

module.exports.items = ['item1', 'item2'];
const person = {
    name: "Boban",
}

module.exports.singlePerson = person; // Exportiert die Variable singlePerson, die eine Kopie von der Konstante person ist.